{% extends 'base.html.twig' %}



{% block body %}
<main class="container ">
    <section class="row">
        <div class="col-12 titreCustom">
            <h1>Bienvenue sur Saccahrose</h1>
            <h2> Saccahrose est un site de vente de pâtisserie, recettes et ustensil de cuisine.</h2>
        </div>
        {% for category in categorie %}
        {% if category.parent == null %}
    
        <div class="col-12 titreCustom">
          <h2>{{ category.name}}</h2>
        </div>
        {% else %}
            <div class="col-6">
                <div class="card" style="width: 18rem;">
                    <img src="{{asset('assets/uploads/products/mini/300x300-'  )}}" class="card-img-top" alt="...">
                    <div class="card-body cardCustom">
                        <h3 class="card-title">{{category.name}}</h3>
                            <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                        <a href="{{ path('categorie_list', {slug: category.slug}) }}" class="btn btnCustom">Voir les produits</a>
                    </div>
                </div>
            </div>
            {% endif %}
        {% endfor %}

        <div class="col-12">
        </div>
    </section>
</main>

<style>
body{
    
   
}
</style>



{% endblock %}